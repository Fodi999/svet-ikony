NEXT_PUBLIC_API_URL=https://ministerial-yetta-fodi999-c58d8823.koyeb.app
NEXT_PUBLIC_SITE_URL=https://svetikony.com

# Streamed real Earth for /pravoslavna-istoriya (Cesium ion imagery, World Terrain, OSM Buildings).
# Public by design (it ships to the browser): create a dedicated token in the Cesium ion dashboard, restrict it to
# your site URLs and to the assets it needs. Without it production keeps the self-hosted NASA layer and makes no ion request.
# NEXT_PUBLIC_CESIUM_ION_TOKEN=
# Set to 0 to skip the Google Maps 2D attempt and use Cesium World Imagery (Bing) directly.
# NEXT_PUBLIC_CESIUM_GOOGLE_2D=
